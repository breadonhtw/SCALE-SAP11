# Credentials — live outside the repo in ~/Desktop/SAP/team-11/; never commit
*credentials*.json
.env
*.env

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/

# Local SQLite fallback DB (CLAUDE.md §18 dev/demo only, never the record of truth)
data/

# macOS
.DS_Store
